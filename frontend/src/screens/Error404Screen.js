//Esto es lo que aparecera al darle clic a una imagen y no exista la pagina
const Error404Screen = {
    render:() => {
        return `<div>Page not found</div>`;
    },
};
export default Error404Screen;